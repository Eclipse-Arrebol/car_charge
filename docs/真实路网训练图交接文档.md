# 真实路网训练图交接文档

## 1. 当前目标

为 FedGRL 构建一套可复现的真实路网图抽象流程：

- `L0 真实路网层`：真实可通行主干路网
- `L1 训练决策层`：由 L0 规则化简得到的训练图
- 维护 `L0 -> L1` 的节点映射，保证训练图仍可追溯到真实图

当前工作重点不是接入训练，而是先把“真实图 -> 训练图 -> 评估判据”这条链路做扎实。

当前 `L1` 的节点语义采用“代表锚点”口径：

- 每个 cluster 在 `L0` 中选择一个代表节点 `r(cluster)`
- `L1` 节点位置使用该代表节点坐标
- 映射可理解为 `pi(u) = r(cluster(u))`

这样 `L1` 的节点不再是纯虚拟质心，而是 `L0` 节点子集上的语义代表点。

---

## 2. 当前结论

当前冻结基线目录：

- [baseline_eps40_artifacts](</g:/交电demo/map_outputs/baseline_eps40_artifacts>)

后续如果要接训练、做论文配图或复现实验，优先从这个目录取文件，而不是再从 `map_outputs/` 根目录零散查找。

### 2.1 推荐默认配置

基于固定采样对和统一 fidelity envelope 的参数扫描，当前推荐：

- `eps* = 40m`

采用的约束为：

- `short cross-cluster P95 <= 25%`
- `medium cross-cluster P95 <= 10%`
- `long cross-cluster P95 <= 10%`
- `short same-cluster rate <= 10%`

在满足上述约束的前提下，`eps=40m` 具有最大压缩率，因此被选为当前默认推荐值。

### 2.2 当前最重要判断

1. `L0` 图在下载阶段已经被 OSMnx 做过简化，因此 `Topology` 层基本是 `no-op`
2. 真正起主要压缩作用的是 `L1` 的局部聚合
3. 之前 fidelity 异常高的主要原因是评估脚本把 `pi(u) == pi(v)` 的 pair 直接算成了 100% 误差
4. 修复该口径后，当前图抽象结果已经进入“参数折中”阶段，而不是“实现 bug”阶段

---

## 3. 关键脚本

### 3.1 下载真实图

文件：

- [download_map.py](/g:/交电demo/map_tools/download_map.py)

作用：

- 下载广州天河区主干可通行路网
- 只保留 EV / 机动车可行驶主干道路
- 保存 `graphml`
- 输出预览图

运行：

```powershell
python -u map_tools/download_map.py
```

主要产物：

- [guangzhou_tianhe_mainroads.graphml](/g:/交电demo/map_outputs/guangzhou_tianhe_mainroads.graphml)
- [guangzhou_tianhe_mainroads_preview.png](/g:/交电demo/map_outputs/guangzhou_tianhe_mainroads_preview.png)

### 3.2 构建训练图

文件：

- [graph_abstraction.py](/g:/交电demo/map_tools/graph_abstraction.py)

作用：

- 加载 `L0`
- 构建 `Topology` 图
- 根据 `eps` 聚合为 `L1`
- 输出训练图、映射文件和对比图

当前默认常量：

- `INTERSECTION_TOLERANCE_M = 35`
- `CLUSTER_MAX_PATH_M = 55`

说明：

- 当前脚本默认输出的是 `stage2` 版本文件
- `eps=40` 的推荐图已经单独导出，见本文第 5 节

运行：

```powershell
python -u map_tools/graph_abstraction.py
```

如果要看 `eps=40` 版本，应使用单独生成的产物，而不是默认 `stage2` 图。

### 3.3 评估训练图保真度

文件：

- [graph_fidelity_eval.py](/g:/交电demo/map_tools/graph_fidelity_eval.py)

作用：

- 分层采样 `short / medium / long`
- 计算 `cross-cluster` 最短路相对误差
- 统计 `same-cluster rate`
- 统计 `intra-cluster` 真实距离分布

运行：

```powershell
python -u map_tools/graph_fidelity_eval.py
```

主要产物：

- [guangzhou_tianhe_stage2_fidelity_report.json](/g:/交电demo/map_outputs/guangzhou_tianhe_stage2_fidelity_report.json)

### 3.4 扫描 eps 参数

文件：

- [graph_eps_scan.py](/g:/交电demo/map_tools/graph_eps_scan.py)

作用：

- 固定一组采样 pair
- 扫描 `eps ∈ {15, 20, 25, 30, 35, 40}`
- 输出每档图压缩率、same-cluster rate、cross-cluster P95 和 edge length 一致性

运行：

```powershell
python -u map_tools/graph_eps_scan.py
```

主要产物：

- [guangzhou_tianhe_eps_scan_results.json](/g:/交电demo/map_outputs/guangzhou_tianhe_eps_scan_results.json)
- [guangzhou_tianhe_scan_pairs.json](/g:/交电demo/map_outputs/guangzhou_tianhe_scan_pairs.json)
- [guangzhou_tianhe_eps_scan_plot_a.png](/g:/交电demo/map_outputs/guangzhou_tianhe_eps_scan_plot_a.png)
- [guangzhou_tianhe_eps_scan_plot_b.png](/g:/交电demo/map_outputs/guangzhou_tianhe_eps_scan_plot_b.png)
- [guangzhou_tianhe_eps_scan_plot_c.png](/g:/交电demo/map_outputs/guangzhou_tianhe_eps_scan_plot_c.png)

---

## 3.5 当前训练与评估命令

### 当前实验口径

- 训练对比组：
  - `G1 (L0)`：在 `L0` 真实图上训练
  - `G2 (L1)`：在 `L1 (eps=40)` 训练图上训练
- 评估口径：
  - 两组都统一在 `L0` 上评估
- 当前默认充电站数：
  - `8`

### 训练命令

训练规模参数：

| 档位 | num_evs | episodes | steps_per_episode | fed_rounds_per_episode | batch_size | step_local_train_steps | step_train_interval | epsilon_final |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `debug` | 10 | 20 | 300 | 5 | 8 | 2 | 1 | 0.80 |
| `ablation` | 40 | 30 | 600 | 3 | 128 | 1 | 4 | 0.90 |
| `medium` | 60 | 100 | 1200 | 5 | 256 | 1 | 4 | 0.90 |
| `full` | 100 | 500 | 3000 | 1 | 256 | 1 | 2 | 0.10 |

快速 sanity check：

```powershell
python main.py train-real --graph-group l0 --debug
python main.py train-real --graph-group l1 --debug
```

中等规模：

```powershell
python main.py train-real --graph-group l0 --medium
python main.py train-real --graph-group l1 --medium
```

消融专用档位：

```powershell
python main.py train-real --graph-group l0 --ablation
python main.py train-real --graph-group l1 --ablation
```

完整规模：

```powershell
python main.py train-real --graph-group l0
python main.py train-real --graph-group l1
```

模型文件命名：

- `l0`：`checkpoints/trained_federated_dqn_real_l0.pth`
- `l1`：`checkpoints/trained_federated_dqn_real_l1.pth`

中间检查点：

- `trained_federated_dqn_real_l0_ep{N}.pth`
- `trained_federated_dqn_real_l1_ep{N}.pth`

### 评估命令

评估规模参数：

| 档位 | num_evs | episodes | steps_per_episode | num_stations | 评估图 |
|---|---:|---:|---:|---:|---|
| `debug` | 100 | 5 | 300 | 8 | `L0` |
| `ablation` | 100 | 10 | 600 | 8 | `L0` |
| `medium` | 100 | 20 | 1200 | 8 | `L0` |
| `full` | 100 | 50 | 3000 | 8 | `L0` |

快速评估：

```powershell
python main.py evaluate --graph-group l0 --debug
python main.py evaluate --graph-group l1 --debug
```

中等规模评估：

```powershell
python main.py evaluate --graph-group l0 --medium
python main.py evaluate --graph-group l1 --medium
```

消融专用档位评估：

```powershell
python main.py evaluate --graph-group l0 --ablation
python main.py evaluate --graph-group l1 --ablation
```

完整评估：

```powershell
python main.py evaluate --graph-group l0
python main.py evaluate --graph-group l1
```

说明：

- `--graph-group l0/l1` 只影响加载哪个训练模型
- 评估环境始终使用 `L0`
- 当前评估默认关闭“充满后重新变成低 SOC 继续参与”的机制，即：
  - `respawn_after_full_charge = False`

### 结果目录

训练结果按图组、规模、时间戳分目录保存：

```text
results/real/<graph_group>/<scale>/<timestamp>/
```

例如：

- `results/real/l0/debug/...`
- `results/real/l1/medium/...`

目录内主要文件：

- `training_curves.png`
- `reward_distribution.png`
- `learning_progress.png`
- `training_data.json`
- `training_summary.txt`

评估结果按图组、规模、时间戳分目录保存：

```text
evaluation/results/<graph_group>/<scale>/<timestamp>/
```

例如：

- `evaluation/results/l0/debug/...`
- `evaluation/results/l1/medium/...`

目录内主要文件：

- `evaluation_report_random.json`
- `evaluation_report_greedy.json`
- `evaluation_report_dqn.json`
- `evaluation_report_feddqn.json`

### 服务器拉取代码前的备份

服务器上在 `git pull` 前，建议先运行：

```bash
bash script/backup_cloud_artifacts.sh
```

该脚本当前会备份：

- `evaluation/results/`
- `results/real/`
- `checkpoints/trained_federated_dqn_real*.pth`
- `checkpoints/trained_dqn_real*.pth`

避免未跟踪实验结果阻塞 `git pull`。

---

## 4. 当前关键数据

### 4.1 L0 真实图

- 节点数：`1582`
- 边数：`2491`

文件：

- [guangzhou_tianhe_mainroads.graphml](/g:/交电demo/map_outputs/guangzhou_tianhe_mainroads.graphml)

### 4.2 Degree 分布诊断

按普通度数统计：

- `Counter({3: 1050, 4: 363, 2: 87, 1: 53, 5: 22, 6: 6, 7: 1})`

按唯一邻居统计：

- `Counter({3: 1044, 4: 357, 2: 100, 1: 54, 5: 21, 6: 5, 7: 1})`

解释：

- 度为 2 的点本来就不多
- 因此这张 `L0` 图已经比较接近拓扑图
- `simplify_graph` 在这里不会产生显著压缩

### 4.3 当前 stage2 基线

- `L0 Raw`：`1582` 节点，`2491` 边
- `Stage2 Training`：`1166` 节点，`1851` 边

文件：

- [guangzhou_tianhe_training_stage2.graphml](/g:/交电demo/map_outputs/guangzhou_tianhe_training_stage2.graphml)
- [guangzhou_tianhe_training_stage2.png](/g:/交电demo/map_outputs/guangzhou_tianhe_training_stage2.png)
- [guangzhou_tianhe_stage2_comparison.png](/g:/交电demo/map_outputs/guangzhou_tianhe_stage2_comparison.png)
- [guangzhou_tianhe_stage2_mapping.json](/g:/交电demo/map_outputs/guangzhou_tianhe_stage2_mapping.json)

### 4.4 修复后的 fidelity 基线

`cross-cluster metrics`：

- `short`：mean `0.0652`，median `0.0439`，P95 `0.2041`，P99 `0.4074`
- `medium`：mean `0.0363`，median `0.0339`，P95 `0.0815`，P99 `0.1084`
- `long`：mean `0.0341`，median `0.0330`，P95 `0.0646`，P99 `0.0824`
- `all`：mean `0.0450`，median `0.0341`，P95 `0.1229`，P99 `0.2469`

`same-cluster`：

- `short same_cluster_rate = 0.0200`
- 同簇短程对在 L0 上的平均距离 `56.04m`
- 同簇短程对在 L0 上的最大距离 `99.48m`

解释：

- `medium/long` 已经进入较理想区间
- `short` 仍存在可解释的局部模糊化
- 这不是实现 bug，而是图抽象带来的真实折中

---

## 5. 当前推荐图：eps=40

这是当前按 envelope 机械选出来的推荐训练图。

产物：

- [guangzhou_tianhe_training_eps40.graphml](/g:/交电demo/map_outputs/guangzhou_tianhe_training_eps40.graphml)
- [guangzhou_tianhe_training_eps40.png](/g:/交电demo/map_outputs/guangzhou_tianhe_training_eps40.png)
- [guangzhou_tianhe_training_eps40_mapping.json](/g:/交电demo/map_outputs/guangzhou_tianhe_training_eps40_mapping.json)

规模：

- 节点：`1122`
- 边：`1782`

扫描结果摘要：

| eps | nodes | ratio | short P95 | medium P95 | long P95 | short same |
|---|---:|---:|---:|---:|---:|---:|
| 15 | 1425 | 0.901 | 0.0492 | 0.0179 | 0.0117 | 0.0000 |
| 20 | 1350 | 0.853 | 0.1502 | 0.0258 | 0.0177 | 0.0000 |
| 25 | 1302 | 0.823 | 0.1510 | 0.0417 | 0.0276 | 0.0100 |
| 30 | 1231 | 0.778 | 0.1740 | 0.0605 | 0.0438 | 0.0100 |
| 35 | 1166 | 0.737 | 0.2041 | 0.0815 | 0.0646 | 0.0200 |
| 40 | 1122 | 0.709 | 0.2386 | 0.0985 | 0.0767 | 0.0300 |

结论：

- 六档都满足既定 envelope
- `eps=40` 压缩率最高
- 因此当前推荐用 `eps=40` 作为下一阶段默认图

---

## 6. 已定位并修复的问题

### 6.1 fidelity 评估口径 bug

旧问题：

- 若 `pi(u) == pi(v)`，会出现 `d1=0`
- 导致 `err = 1.0`
- 使 short 桶 P95 人工飙高

修复后：

- `same-cluster` pair 单独统计
- 主结果使用 `cross-cluster metrics`
- 同时输出 `same_cluster_rate` 和 `intra_cluster_stats`

### 6.2 segment 多候选边问题

已经做过专项诊断：

- 当前这版图中，每个 `cluster-pair` 只有 `1` 条 segment
- 因此当前版本不存在“多候选边被错误取 min”的实际触发问题

备注：

- 这是当前数据上的结论
- 若后续更换城市或更换下载规则，建议重新跑一次 segment 诊断

---

## 7. 建议的下一步

### 7.1 如果继续图抽象主线

优先顺序：

1. 将 `eps=40` 固化为默认训练图配置
2. 整理正式的层间映射结构
3. 把 `L0/L1` 的说明写进方法章节

### 7.2 如果准备接入训练

建议顺序：

1. 先固定 `L1 = eps40` 这张图
2. 在环境里明确：
   - 训练使用 `L1`
   - 真实代价和最终评估回到 `L0`
3. 先跑通一版最小训练实验，再决定是否需要继续优化图抽象

### 7.3 当前不建议继续做的事

- 不建议继续无上限地扫描更多 `eps`
- 不建议继续反复人工看图调参数
- 不建议在未固定 `L1` 前就接训练和评估，否则实验口径会漂

---

## 8. 一句话交接

当前真实路网抽象链路已经跑通，`fidelity` 评估口径也已修正；按照既定约束，当前推荐训练图为 `eps=40`，对应文件是 [guangzhou_tianhe_training_eps40.graphml](/g:/交电demo/map_outputs/guangzhou_tianhe_training_eps40.graphml)，下一步可以直接进入“固化默认配置”或“接入训练”。
