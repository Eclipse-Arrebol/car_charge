# 下一步规划

---

## 当前已完成的核心改动（本轮）

| 改动 | 文件 | 说明 |
|------|------|------|
| Epsilon 衰减修复 | `visualization/run_training_real_map.py`、`main.py` | medium 训练结束时 ε 从 0.990 降到 0.10 |
| FedProx μ=0.01 | `visualization/run_training_real_map.py` | 有效近端约束 |
| FeatureEncoder + Dueling | `agents/GraphQNetwork.py` | 分组编码 + Q 值解耦 |
| DP-SGD + RDPAccountant | `agents/FederatedDQN.py` | 差分隐私支持 |
| Greedy 基线 | `evaluation/run_evaluation.py` | 四方对比 |

---

## 阶段一：补齐实验数据（最优先）

### 1. 重跑 medium 训练（epsilon 修复后）

```bash
python main.py train-real --medium
python main.py evaluate --medium
```

预期变化：
- epsilon 由 0.990 → 0.10，模型真正开始利用学到的策略
- FedDQN wait_time 预计比 -26% 进一步提升

### 2. DP 三组对比实验

目标：获得 ε-性能权衡曲线，写入论文 Table。

**σ=0.3（弱隐私保护，ε 较大）：**
```bash
python main.py train-real --medium --dp
# 修改 run_training_real_map.py 中 dp_noise_multiplier=0.3 后运行
python main.py evaluate --medium
```

**σ=1.0（中等，已有结果）：** wait_time +5%，记录 ε 值即可。

**σ=2.0（强隐私保护，ε 较小）：**
```bash
# 修改 dp_noise_multiplier=2.0 后运行
python main.py train-real --medium --dp
python main.py evaluate --medium
```

> 待办：将 `--dp-sigma` 做成命令行参数，方便切换。

三组结果整理成：

| σ | ε（隐私预算） | avg_wait_time 变化 | abandoned_evs 变化 |
|---|-------------|-------------------|-------------------|
| 0.3 | 待填 | 待填 | 待填 |
| 1.0 | 待填 | +5% | +9% |
| 2.0 | 待填 | 待填 | 待填 |

---

## 阶段二：代码完善（中优先）

| 任务 | 说明 | 预计改动 |
|------|------|---------|
| `--dp-sigma` 命令行参数 | 方便切换 σ 跑三组实验 | `main.py` |
| 充电站数量从 2 增加到 4 | 图结构优势更明显，Greedy 更难赢 | `run_training_real_map.py`、评估脚本 |

---

## 阶段三：论文写作（高优先，可与阶段一并行）

| 章节 | 状态 | 待补内容 |
|------|------|---------|
| 模型章 | 有初稿（`docs/模型章初稿.md`） | 补 FeatureEncoder 分组编码、Dueling Head 描述 |
| 实验章 | 无 | 用四轮实验结果填表，写 ε-性能曲线分析 |
| 结论章 | 无 | 三个贡献点总结 |

### 实验章核心表格（待填）

**Table 1：四策略对比（无 DP）**

| 指标 | Random | Greedy | DQN | FedDQN |
|------|--------|--------|-----|--------|
| avg_wait_time_min | 223.20 | 176.55 (-21%) | 197.10 (-12%) | **164.40 (-26%)** |
| abandoned_evs | 39.50 | 30.55 (-23%) | 33.25 (-16%) | **28.25 (-28%)** |
| total_charging_cost | 1101.83 | 1113.84 (+1%) | **1062.96 (-4%)** | 1100.79 (-0%) |

**Table 2：FedGRL DP 权衡曲线（待填）**

| σ | ε | avg_wait_time 变化 | 说明 |
|---|---|-------------------|------|
| 无 DP | ∞ | -26% | 最优性能 |
| 0.3 | 待填 | 待填 | 弱保护 |
| 1.0 | 待填 | +5% | 中等保护 |
| 2.0 | 待填 | 待填 | 强保护 |

---

## 建议执行顺序

```
① python main.py train-real --medium        ← 修复 epsilon 后重跑
② python main.py evaluate --medium          ← 拿新的对比数字
③ 开始写模型章（补 FeatureEncoder/Dueling 描述）
④ 跑 DP σ=0.3 和 σ=2.0 实验              ← 拿 ε-性能曲线数据
⑤ 写实验章（填 Table 1 和 Table 2）
```
